# SensorTrain host bridge configuration

PICO_DEVICE=/dev/sensortrain-pico

# Replace SENSORTRAIN_SERVICE_IP with the ClusterIP
# assigned to the sensortrain-web Service.
TELEMETRY_URL=http://SENSORTRAIN_SERVICE_IP:8080/api/telemetry
CONTROL_URL=http://SENSORTRAIN_SERVICE_IP:8080/api/control
